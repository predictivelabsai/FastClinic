[pytest]
# Agent evals run through ``python -m evals.run_agent_eval``. Loading DeepEval's
# pytest plugin here would auto-load the deployment .env during test collection
# and load deployment configuration during test collection. tests/conftest.py
# independently isolates database paths from local Docker-oriented .env values.
addopts = -p no:deepeval
